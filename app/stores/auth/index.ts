import resourceReducer, { setUser, setToken, checkToken } from "./reducers"

export {
  selectResource
} from './selectors'

export {
  setUser,
  setToken,
  checkToken
}

export default resourceReducer